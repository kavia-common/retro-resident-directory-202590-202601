# Frontend environment variables (React)
# Set this to the backend base URL when frontend and backend are on different origins.
# Example (local dev): http://localhost:3001
# Example (Kavia): https://<your-backend-host>
REACT_APP_API_BASE_URL=
